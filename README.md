# terraform-samples
> Configuration Samples for Terraform.


## Requirements
* aws-cli  
    ```
    brew install awscli
    aws configure
    ```
* terraform
    ```
    brew install terraform
    ```
