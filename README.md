# terraform-examples
> Examples for Terraform Configuration.


## Requirements
* aws-cli  
    ```
    brew install awscli
    aws configure
    ```
* terraform
    ```
    brew install terraform
    ```
